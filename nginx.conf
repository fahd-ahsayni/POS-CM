server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Serve static files directly from the build
    # location /static/ {
    #     expires 1y;
    #     # add_header Cache-Control "public, no-transform";
    # }

    # # Proxy API requests to the backend
    # location /api/ {
    #     proxy_pass http://192.168.1.5:8000;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_cache_bypass $http_upgrade;
    # }

    # Handle SPA 404 errors
    error_page 404 /index.html;

    # Add Content Security Policy headers
    # add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' blob:; worker-src 'self' blob:;" always;
}
